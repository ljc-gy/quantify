name = "quantify"
pages_build_output_dir = "quant-dashboard/client/dist"
compatibility_date = "2025-06-15"
compatibility_flags = ["nodejs_compat"]

# D1 database — created via: wrangler d1 create quantify-db
[[d1_databases]]
binding = "DB"
database_name = "quantify"
database_id = "a4cd34c3-80fc-4062-b688-031b2b948d50"

# Cloudflare Pages project — deploys Vite frontend + Hono API via Functions
